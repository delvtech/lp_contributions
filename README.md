create a virtual environment like with `python -m venv .venv`

install python dependencies with `pip install -r requirements.txt`

copy `.env.example` to `.env` and enter your details

run `python lp_distribution.py`
